version: "3.9"

services:
  scholarhub:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: scholarhub-app
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: unless-stopped
